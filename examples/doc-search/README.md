# doc-search — an example tendrel graph

A small, domain-neutral example so you can see what a real tendrel graph looks like without
installing anything. Browse `graph/` for the typed nodes, or open [`status.md`](status.md) —
GitHub renders its mermaid diagram inline, so you get a live view of the graph (theories,
dependency chains, and an invalidated node with a blocked node downstream of it).
